// API Configuration
// This file centralizes the API URL so it only needs to be changed in one place

const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000/api';

export default API_URL;
